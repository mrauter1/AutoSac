# Plan ↔ Plan Verifier Feedback

- 2026-04-10 planner: Replaced the placeholder artifacts with a three-phase implementation plan anchored to the current Slack modules, covering the additive schema change, explicit SlackRuntimeContext propagation, first-class routing snapshot persistence, claim-token delivery ownership, the single finalization boundary, and the regression/rollout controls required by the refactor PRD.
- PLAN-001 | non-blocking | Re-review complete: no blocking findings in this verifier pass. The plan and phase decomposition stay aligned with the refactor PRD, keep the change inside the existing Slack modules, make the pre-launch no-compatibility posture explicit, and cover the main regression surfaces around runtime propagation, duplicate reuse, claim-token ownership, and rollout.
- 2026-04-10 planner: Replaced the stale webhook-refactor plan with a four-phase Slack DM rollout plan grounded in the current FastAPI, SQLAlchemy, and worker surfaces. The new plan makes the DB-backed singleton config, token encryption, admin-only integration UI, admin-managed user Slack IDs, DM recipient routing, Web API delivery, disposable pre-launch migration posture, and required doc/test rewrites explicit.
- PLAN-002 | blocking | Admin-surface intent coverage is still incomplete. In `plan.md` and `phase_plan.yaml`, the Slack integration page is planned generically, but the PRD also requires the page to document the required Slack bot scopes and requires the disconnect action to explicitly disable delivery, clear the stored ciphertext, and state whether non-secret workspace metadata is preserved or intentionally cleared. Without those behaviors being explicit in the plan, the implementation can ship an incomplete admin contract even if the routes exist. Minimal correction: add the scope-documentation requirement and the exact disconnect persistence behavior to the plan narrative, phase deliverables, and acceptance criteria.
- PLAN-003 | blocking | Recipient-routing safety is underspecified. `plan.md` defines requester/assignee candidate selection and collapse, but it does not state the PRD's mandatory no-actor-suppression rule. That omission leaves room for an implementer to exclude self-notifications for the acting requester or assignee, which would silently change the DM routing contract. Minimal correction: add an explicit routing rule that recipient selection is requester/assignee-based only and must not suppress notifications just because the recipient performed the action.
- PLAN-004 | blocking | DM-specific delivery observability is not explicit enough. The PRD requires delivery logs to include `recipient_user_id` and `recipient_reason`, but the plan only updates emission logging to `recipient_target_count` and leaves the worker-side logging contract implicit. That creates a concrete regression path where the implementation keeps the old `target_name`-centric worker logs after moving to recipient-based DM rows. Minimal correction: add recipient-user and recipient-reason fields to the worker claim/result logging requirements and to the regression test coverage.
