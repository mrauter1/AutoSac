<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260410T184024Z-448797a6" ts="2026-04-10T18:40:24.610259+00:00" />
Keep the refactor inside shared/integrations.py, shared/ticketing.py, and worker/slack_delivery.py with small typed helpers and context objects rather than introducing a new Slack package or DI framework.
Thread explicit Slack runtime through Slack-emitting ticketing helpers, especially record_status_change, publish_ai_public_reply, add_ops_public_reply, and publish_ai_draft_for_ops, because those paths currently depend on ambient session settings.
Do not plan any compatibility bridge for payload _integration_routing metadata or processing rows with claim_token unset; deployment relies on SLACK_ENABLED=false and disposable pre-launch Slack integration rows instead.
<autoloop-decisions-header version="1" block_seq="2" owner="implementer" phase_id="schema-emission-runtime-boundary" pair="implement" turn_seq="1" run_id="run-20260410T184024Z-448797a6" ts="2026-04-10T18:46:33.985041+00:00" />
Require an explicit SlackRuntimeContext on Slack-emitting ticketing and integration helpers, then construct it once per route or worker flow from the already-resolved Settings instead of letting helpers infer settings from the SQLAlchemy session.
Read duplicate-routing outcomes only from integration_events routing columns and existing target rows; zero-target duplicates degrade to suppressed_notify_disabled whenever the stored snapshot is missing or still says created.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="delivery-claim-outcome-finalization" pair="implement" turn_seq="1" run_id="run-20260410T184024Z-448797a6" ts="2026-04-10T19:04:50.758249+00:00" />
Construct a SlackRuntimeContext once at the Slack delivery thread boundary and adapt its logger to worker logging so delivery code uses the same explicit runtime shape as emission without changing the existing worker log contract.
Make finalize_delivery_claim the only post-claim row-mutation owner; it applies the supplied DeliveryOutcome write set by claim_token ownership only and never recomputes retry exhaustion or next_attempt_at.
Use stable HTTP failure-class buckets in executor outcomes (`http_408`, `http_429`, `http_5xx`, `http_3xx`, `http_4xx`) while preserving the existing sanitized last_error summaries for operator logs and persisted state.
<autoloop-decisions-header version="1" block_seq="4" owner="test_author" phase_id="delivery-claim-outcome-finalization" pair="test" turn_seq="1" run_id="run-20260410T184024Z-448797a6" ts="2026-04-10T19:14:28.990663+00:00" />
Cover run_delivery_cycle orchestration with monkeypatched repository/executor helpers so the tests pin claim-token claim logging and SlackRuntimeContext threading without duplicating lower-level state-transition assertions already covered in delivery-unit tests.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="regression-rollout-verification" pair="implement" turn_seq="1" run_id="run-20260410T184024Z-448797a6" ts="2026-04-10T19:16:50.552162+00:00" />
Keep rollout-facing docs and checks explicit about refactor-era deployment: `SLACK_ENABLED=false` through migration and verification, web/request-path plus worker shipped together, and any pre-refactor Slack integration rows treated as disposable pre-launch state before enablement.
Remove dormant `Session.info["settings"]` scaffolding from the Slack emission test harness so regression coverage no longer carries a legacy ambient-settings path that production code does not support.
<autoloop-decisions-header version="1" block_seq="6" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260410T195822Z-068ccbe4" ts="2026-04-10T19:58:22.678954+00:00" />
Replace the webhook-era env-backed Slack runtime contract entirely; Slack DM routing and delivery must load their authoritative config from PostgreSQL, with APP_SECRET_KEY retained only for bot-token encryption and decryption.
Use one small shared Slack DM helper module for singleton config load/save, token crypto, Slack Web API calls, and last-known delivery health, while keeping event emission in shared/integrations.py, worker delivery orchestration in worker/slack_delivery.py, and admin UI in app/routes_ops.py.
Treat existing integration_events, integration_event_links, and integration_event_targets rows as disposable pre-launch Slack state during the DM migration so recipient_user_id, recipient_reason, and target_kind=slack_dm can be enforced without a compatibility bridge.
Persist worker-observed Slack delivery health in system_state so /ops/integrations/slack can show last-known auth or health state without making live Slack calls during page render.
Supersedes the earlier webhook-refactor guidance to construct SlackRuntimeContext once at the delivery thread boundary; the DM worker must reload DB-backed Slack config and token state each cycle.
Supersedes earlier rollout guidance that used SLACK_ENABLED or other SLACK_* env knobs for enablement; enable, disable, notify flags, and delivery tuning now live in slack_dm_settings.
<autoloop-decisions-header version="1" block_seq="7" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260410T195822Z-068ccbe4" ts="2026-04-10T20:08:28.875200+00:00" />
Document the required Slack bot capabilities and scopes directly on /ops/integrations/slack, specifically the auth.test, conversations.open, and chat.postMessage behavior needed for DM delivery.
The disconnect action will disable Slack DM delivery, clear bot_token_ciphertext, and preserve non-secret workspace and audit metadata by default instead of clearing it implicitly.
Recipient routing is recipient-based only; requester or assignee users must not be suppressed just because they performed the triggering action themselves.
Worker claim and delivery-result logs must include recipient_user_id and recipient_reason so DM observability does not regress after replacing target-name webhook delivery.
<autoloop-decisions-header version="1" block_seq="8" owner="implementer" phase_id="dm-persistence-runtime-foundation" pair="implement" turn_seq="1" run_id="run-20260410T195822Z-068ccbe4" ts="2026-04-10T20:10:46.134380+00:00" />
Keep `Settings.slack` as a non-authoritative defaults and test-injection surface only; all production request-path and worker runtime construction now calls `build_slack_runtime_context(..., db=db)` or per-cycle worker loading so PostgreSQL is the only authoritative Slack DM source.
Make the foundation migration clear disposable pre-launch Slack rows but keep `integration_event_targets.target_kind` additive (`slack_webhook` and `slack_dm`) with recipient-coupling checks, so this phase can land the DM schema without forcing partial recipient-routing or send-transport behavior ahead of the later phases that own those changes.
<autoloop-decisions-header version="1" block_seq="9" owner="implementer" phase_id="dm-persistence-runtime-foundation" pair="implement" turn_seq="2" run_id="run-20260410T195822Z-068ccbe4" ts="2026-04-10T20:30:08.451719+00:00" />
Tighten the DM foundation to `target_kind=slack_dm` now that the migration already deletes pre-launch Slack integration rows; do not preserve a webhook compatibility branch in the persisted schema.
Mark DB-loaded Slack settings as `routing_mode='dm'` and have the request-path runtime emit `suppressed_no_recipients` with zero target rows until the later recipient-routing phase lands, while keeping direct `Settings.slack` injection on the legacy webhook-mode path for existing non-DB unit tests only.
<autoloop-decisions-header version="1" block_seq="10" owner="test_author" phase_id="dm-persistence-runtime-foundation" pair="test" turn_seq="1" run_id="run-20260410T195822Z-068ccbe4" ts="2026-04-10T20:33:12.368088+00:00" />
Cover missing-row defaults, blank-token preservation, and clear-token metadata retention in `tests/test_slack_dm_foundation.py` at the shared helper layer; these behaviors are phase-in-scope runtime contracts and should not wait for later admin-route tests.
<autoloop-decisions-header version="1" block_seq="11" owner="implementer" phase_id="admin-ui-and-user-mapping" pair="implement" turn_seq="1" run_id="run-20260410T195822Z-068ccbe4" ts="2026-04-10T20:36:18.779524+00:00" />
Reject non-admin `slack_user_id` submissions at the `/ops/users` route layer whenever the field is present, including blank-string clear attempts, so Dev/TI users cannot mutate the mapping through handcrafted form posts.
On `/ops/integrations/slack` validation failures, re-render the form with the submitted non-secret values but always reload persisted token-presence and metadata from PostgreSQL and keep the bot-token input blank so save errors never echo secrets back into HTML.
