<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T13:16:21.019577+00:00" />
Slack-specific config parsing and validation must be soft and must not block app or worker startup; invalid Slack config suppresses routing and delivery instead.
Keep all Phase 1 event persistence behind one shared integration helper/module and invoke it only from the existing central mutation helpers so duplicate and no-event rules stay consistent.
Treat duplicate dedupe_key reuse as read-only for preexisting integration rows; do not repair or mutate existing link/target state during later duplicate emissions driven by new config.
Run Slack delivery in a dedicated worker thread with separate DB sessions rather than piggybacking on the AI-run polling loop.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T13:23:15.437891+00:00" />
Internal-note and draft rows themselves are not Phase 1 emission sources, but AI failure, AI internal route-only, and draft-creation flows still emit ticket.status_changed whenever record_status_change commits a distinct non-initial status transition.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="schema-and-config-foundation" pair="implement" turn_seq="1" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T13:25:02.639268+00:00" />
Slack-only env parse and range errors fall back to safe defaults but still set `settings.slack.is_valid=false` with the first stable `config_error_code`/`config_error_summary`, so startup remains non-fatal while later routing and delivery can suppress cleanly.
The schema foundation enforces `sent_at`/`dead_lettered_at` delivery-state invariants with DB check constraints in `integration_event_targets` instead of leaving those Phase 1 guarantees entirely to later worker code.
<autoloop-decisions-header version="1" block_seq="4" owner="test_author" phase_id="schema-and-config-foundation" pair="test" turn_seq="1" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T13:38:51.075844+00:00" />
Schema/config foundation test coverage stays at migration-source and config-helper level for this phase; broader FastAPI smoke checks are documented as environment-sensitive here because missing `python-multipart` in the runner aborts route import before the Slack assertions execute.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="transactional-event-emission" pair="implement" turn_seq="1" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T13:41:40.256147+00:00" />
Expose resolved `Settings` through `Session.info["settings"]` so web and worker DB sessions can emit integration events without widening every ticketing function signature; lightweight fake sessions still skip emission unless a test injects settings explicitly.
Assign `TicketStatusHistory.id` eagerly inside `record_status_change` so `ticket.status_changed:<status_history_id>` dedupe keys and required status-history links are available before the outer transaction commits.
Persist the initial routing/logging outcome in `integration_events.payload_json["_integration_routing"]` so duplicate emissions can replay the original suppressed-vs-created log state without schema changes or target-row repair.
If a reused event has zero target rows and no preserved routing metadata, duplicate logging must still clamp away from `routing_result="created"` to avoid falsely claiming target creation.
<autoloop-decisions-header version="1" block_seq="6" owner="implementer" phase_id="async-delivery-runtime" pair="implement" turn_seq="1" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T14:03:00.978980+00:00" />
The Slack delivery loop uses separate `session_scope` transactions for stale-lock recovery, batched claims, and per-attempt finalization so claimed `processing` rows are durable before outbound HTTP and remain recoverable after crashes.
Claimed delivery work copies `integration_events.event_type` and `payload_json` into an in-memory claimed-target snapshot, and Slack rendering uses only that snapshot so send-time delivery never reloads current ticket or message rows.
Only invalid Slack config emits per-poll runtime suppression logs; the common `SLACK_ENABLED=false` rollout posture still suppresses claim/send/recovery but stays silent to avoid worker log spam.
<autoloop-decisions-header version="1" block_seq="7" owner="implementer" phase_id="regression-hardening-and-rollout-readiness" pair="implement" turn_seq="1" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T14:25:01.927712+00:00" />
Environment-sensitive health/script checks that import the full FastAPI app are skipped when `python-multipart` is absent in the runner, because this phase hardens Slack coverage and rollout docs without treating an unrelated optional form-parser package gap as a Slack regression.
<autoloop-decisions-header version="1" block_seq="8" owner="test_author" phase_id="regression-hardening-and-rollout-readiness" pair="test" turn_seq="1" run_id="run-20260410T131621Z-1c9c1f3e" ts="2026-04-10T14:30:42.756481+00:00" />
The hardening-suite multipart gate should call FastAPI's own `ensure_multipart_is_installed()` when available so skip behavior matches the framework's actual route-import guard instead of relying only on raw module presence.
