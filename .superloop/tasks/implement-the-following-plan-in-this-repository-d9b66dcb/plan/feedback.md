# Plan ↔ Plan Verifier Feedback

- 2026-03-23: Replaced empty planning artifacts with a seven-phase implementation plan that mirrors the requested work order, names the concrete files/interfaces to change, and records regression controls for browser auth semantics, HTMX fragment behavior, Codex prompt transport, and deterministic bootstrap/system-state seeding.
- PLAN-001 | blocking | [plan.md](/workspace/AutoSac/.superloop/tasks/implement-the-following-plan-in-this-repository-d9b66dcb/plan/plan.md) Auth And Browser UX / Interfaces And Data Changes, [phase_plan.yaml](/workspace/AutoSac/.superloop/tasks/implement-the-following-plan-in-this-repository-d9b66dcb/plan/phase_plan.yaml) `auth-browser-ux`: the plan introduces sanitized `/login?next=...` redirects but never states that successful login must consume the sanitized `next` value and redirect there instead of always using the current role-default destination. That leaves the core browser-return flow ambiguous and can ship a regression where protected-page redirects send users to `/login` but login success still lands on `/ops` or `/app` rather than the originally requested page. Minimal correction: explicitly add login success behavior for sanitized `next` handling, including fallback to `post_login_redirect_path(user)` only when `next` is absent or invalid, and cover it in route tests.
- 2026-03-23: Updated the auth/browser contract in both plan artifacts to resolve PLAN-001 by making sanitized `next` consumption explicit on successful login and logged-in `/login` visits, adding the fallback rule to `post_login_redirect_path(user)`, and extending the auth validation/rollback notes so the redirect producer and consumer stay coupled.
- PLAN-002 | non-blocking | Re-review after the PLAN-001 fix found no remaining blocking gaps: the plan now explicitly covers sanitized `next` production and consumption, keeps browser-only redirect semantics scoped away from JSON/health behavior, and preserves the requested migration, rollback, and regression-test coverage across the remaining workstreams.
