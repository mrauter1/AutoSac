# Plan ↔ Plan Verifier Feedback

- 2026-03-24: Replaced the empty plan with a 3-phase implementation plan covering auth/UI hardening, worker/bootstrap hardening, and docs/regression closure; captured additive preauth-session persistence, safe next/403 invariants, HTMX fragment rules, and bootstrap/idempotency risk controls so implementation can proceed without scope drift.
- PLAN-001 | blocking | Affected sections: `plan.md` -> `Auth And Browser Flow`, `Compatibility, Migration, Rollout, Rollback`. The plan introduces a new server-side preauth login state store for anonymous login CSRF but does not define the concrete security contract for that store: how the browser is tied to the server record, whether the client token remains opaque-only, what expiry window applies, and how stale preauth rows are bounded or cleaned up. Because this is a new persisted auth surface, the omission leaves room for incompatible or unsafe implementations. Minimal correction: specify the preauth record fields and browser transport contract, its expiry/lifetime, and the minimal cleanup/backward-compatibility expectations.
- PLAN-002 | blocking | Affected sections: `phase_plan.yaml` -> `web-auth-ui-hardening`, `worker-bootstrap-hardening`. The ordered phase contract is internally inconsistent: phase 1 owns server-side preauth login CSRF implementation, but phase 2 owns the deliverable `Any additive migration needed for server-side preauth login state` and depends on phase 1. If additive persistence is required for the preauth store, phase 1 cannot complete before its own prerequisite exists. Minimal correction: move the preauth persistence/migration work into phase 1 or split it into an earlier dependency phase that both auth and worker follow.
- 2026-03-24: Addressed PLAN-001 and PLAN-002 by making the preauth-login contract explicit in `plan.md` (opaque `/login` cookie, hashed server-side token, sanitized `next_path`, short TTL, success-time invalidation, opportunistic cleanup, and fail-closed refresh behavior) and by moving the additive preauth persistence/migration work into `web-auth-ui-hardening` so the phase ordering matches the actual dependency chain.
- 2026-03-24 verifier re-review: PLAN-001 and PLAN-002 are resolved in the current artifacts; no additional blocking or non-blocking findings remain.
