<superloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260324T002640Z-1dcbc90f" ts="2026-03-24T00:26:40.375014+00:00" />
HTMX unauthenticated browser-route redirects should keep the same safe login target but switch transport to an `HX-Redirect` response only when `HX-Request` is present; non-HTMX browser requests must stay on the existing 303 flow.
Browser HTML wrong-role handling should be isolated to `require_browser_requester_user` and `require_browser_ops_user` so API/non-browser 403 responses, CSRF failures, and other plain `HTTPException` paths remain unchanged.
<superloop-decisions-header version="1" block_seq="2" owner="implementer" phase_id="browser-auth-regressions" pair="implement" turn_seq="1" run_id="run-20260324T002640Z-1dcbc90f" ts="2026-03-24T00:28:59.564719+00:00" />
HTMX unauthenticated browser redirects are returned as HTTP 200 with only an `HX-Redirect` header so htmx performs a full navigation without following a 3xx fragment swap path.
Browser wrong-role HTML responses use a dedicated `BrowserForbiddenRequired` exception carrying `current_user` and `auth_session`, which keeps the `403.html` page on the normal base-template navigation/logout context while leaving all plain `HTTPException(403)` flows untouched.
<superloop-decisions-header version="1" block_seq="3" owner="test_author" phase_id="browser-auth-regressions" pair="test" turn_seq="1" run_id="run-20260324T002640Z-1dcbc90f" ts="2026-03-24T00:32:10.062215+00:00" />
Preserved non-browser 403 coverage is anchored on logout CSRF and attachment-download assertions remaining plain JSON `HTTPException(403)` responses with no redirect or browser 403 template content, so future changes cannot accidentally widen the browser-only exception handler.
