## Doc-Loop Progress Log

- 2026-04-09 Writer: Rewrote `slack_integration_PRD.md` from note-style recommendations into a normative Phase 1 spec. Centralized the canonical contracts for schema, event emission, payloads, config, delivery state, retry/dead-letter behavior, and rollout. Removed ungrounded file-level assumptions, resolved the initial-ticket/public-message/status-change overlap in one emission section instead of duplicating rules, and tightened the config/runtime contracts so Slack remains fully asynchronous and non-blocking to core ticket behavior.
- 2026-04-09 Verifier: The document is close, but it is not implementation-ready yet. Required writer fixes:
  1. Resolve the contradiction between Section 7.3 and Section 10 on message body exposure. Section 7.3 allows `message_preview` to equal the full normalized `body_text` when the message is short, while Section 10 says delivered Slack messages MUST NOT include full untruncated message bodies. Pick one canonical rule and update both sections so a short public reply is handled unambiguously.
  2. Add explicit Slack-text sanitization rules for all user-derived fields rendered into `text`, at minimum `ticket_title` and `message_preview`. The PRD currently leaves Slack control syntax handling undefined, so one implementation could post raw `<!channel>`, `<@U123>`, or angle-bracket markup from ticket content while another escapes or neutralizes it. Specify the required escaping/neutralization behavior so requesters cannot trigger mentions or Slack markup injection through ticket content.
  3. Define send-time behavior for already-created target rows when the named target is later disabled or removed from `SLACK_TARGETS_JSON`. The PRD covers emission-time routing and global `SLACK_ENABLED = false`, but it does not say whether pending/failed rows for a specific target should still send, pause, or dead-letter when that target now has `enabled = false` or no longer exists. Choose one policy and state the required target-row transition/result.
  4. Tighten the recovery/runtime contract by specifying a finite outbound HTTP timeout and the exact stale-lock recovery requeue state. Right now the PRD says timeouts are retryable and stale locks must become retryable again, but it does not define the required request timeout bound or whether recovered rows become `failed` or `pending` and what happens to `next_attempt_at`. Those choices materially change duplicate-send risk and recovery latency.
- 2026-04-09 Writer: Addressed the verifier queue in `slack_integration_PRD.md`. Kept the short-message body rule canonical in Sections 7.3 and 10 by explicitly allowing `message_preview` to equal the full normalized public body when it fits within the preview limit, while forbidding any Slack body content beyond that preview. Added render-time Slack escaping rules for user-derived fields, defined send-time dead-letter behavior for rows whose named target is later missing or disabled, introduced bounded timeout and stale-lock configuration contracts, aligned `attempt_count` with claim-time increment semantics, and expanded minimum test coverage for the new edge cases.
- 2026-04-09 Verifier: The document is still not implementation-ready. Required writer fixes:
  1. Resolve the `ticket_url` contract ambiguity in Section 7.1. The PRD says the exact route path is implementation-specific, but the base AutoSac PRD already fixes the Dev/TI ticket detail route at `/ops/tickets/{reference}`. That leaves two plausible implementations today: build links with the existing reference-based route, or invent some other absolute/stable path such as `/ops/tickets/{ticket_id}`. Update the Slack PRD so `ticket_url` explicitly targets the existing operator detail route, and state whether the path component is `ticket_reference` or another already-defined identifier.
  2. Fix the contradiction between the main event rules and the minimum-test section for AI failure and draft flows. Section 6.2 says every committed non-initial distinct status transition emits `ticket.status_changed`, which would include Stage 1 actions like AI run failure (`waiting_on_dev_ti`) and draft creation (`waiting_on_dev_ti`) when they actually change status. But Section 12 currently says "AI failure notes, draft creation, and draft rejection emit no Slack event targets." A competent implementer could therefore either emit status-change notifications for those actions or suppress them to satisfy the test. Make the action-to-event mapping canonical: either explicitly exempt those status transitions from `ticket.status_changed`, or update Section 12 so it only suppresses the non-public message events and still expects `ticket.status_changed` when the action performs a real status change.
- 2026-04-09 Writer: Resolved the remaining verifier queue in `slack_integration_PRD.md`. Made `ticket_url` explicit as `<APP_BASE_URL>/ops/tickets/<ticket_reference>` to match the Stage 1 Dev/TI route, updated the illustrative URLs accordingly, and fixed the AI failure/draft contradiction by strengthening the canonical `ticket.status_changed` rule in Section 6.2 instead of duplicating action-specific exceptions in multiple sections. Section 12 now distinguishes "no public-message event" from "status-change event still required when the action commits a distinct transition."
- 2026-04-09 Writer: Tightened `slack_integration_PRD.md` for verifier pass-readiness. Reconciled the webhook delivery guarantee with `.docloop/context.md` by making external Slack posting explicitly at-least-once with one rare duplicate-post window, while keeping event storage and target-row uniqueness exact in PostgreSQL. Also made `message_preview` truncation deterministic, fixed config-invalid pause behavior versus per-target dead-letter behavior, defined retry classification/backoff and stale-lock recovery cadence more precisely, and clarified `last_error`/`locked_by`/logging contracts without duplicating rules across sections.
- 2026-04-09 Verifier: The document is still not implementation-ready. Required writer fixes:
  1. Make `message_author_type` canonical in Section 7.2. The PRD requires this payload field and Section 10 requires it in the rendered Slack message, but the document never says how to derive it. That leaves at least two plausible implementations for approved AI draft publication: emit `ai` because the content originated from AI, or emit `dev_ti` because a human approved and published it. Update the PRD to state the exact derivation rule, for example whether `message_author_type` MUST equal the associated public `ticket_messages.author_type` value, and call out any special case explicitly if `ai_draft_published` is not meant to follow the general rule.
  2. Resolve the duplicate-repair versus routing-change ambiguity between Sections 6.1 and 8.3. Section 6.1 says that when a duplicate `dedupe_key` is encountered and the target row is missing, the implementation must create the missing row when current routing rules require one. Section 8.3 says Phase 1 routes each event to at most one target and later target-config changes must not rewrite existing rows. Those rules still permit conflicting implementations after `SLACK_DEFAULT_TARGET_NAME` changes from A to B: one engineer could create a new `(event_id, B)` row when the same business fact is emitted again, while another could keep the event permanently associated with zero or one original target row. Update the PRD so duplicate repair is canonical: specify whether repair is allowed only when the event currently has zero target rows, which target name must be used for repair, and that later routing changes MUST NOT create a second target row for an already-emitted Phase 1 event.
- 2026-04-09 Writer: Resolved the remaining verifier blockers in `slack_integration_PRD.md`. Made `message_author_type` canonical in Section 7.2 by tying it to the committed public `ticket_messages.author_type` row for `message_id` and explicitly locking `ai_draft_published` to `ai`, matching the existing Stage 1 behavior. Replaced the ambiguous duplicate-repair sentence in Section 6.1 with a zero-target-only repair rule, fixed the routing section to forbid second-target creation after later config changes, and added only brief cross-references/tests rather than duplicating the routing logic in multiple sections.
- 2026-04-09 Verifier: The document is close, but it is still not implementation-ready because the observability contract is internally inconsistent. Required writer fixes:
  1. Reconcile Section 11 with Section 6.1 on log ownership. Section 6.1 says event emission happens inside the same DB transaction as the ticket mutation, while Section 11 says "The worker MUST emit structured logs for: event emission". Those cannot both be true in the current architecture. Update the PRD so event-emission logging is owned by the process that actually performs emission, or make the requirement process-agnostic. Keep one canonical statement.
  2. Split or narrow the required structured-log fields in Section 11. The current rule says all structured Slack-delivery logs MUST include `event_id`, `target_name`, `delivery_status`, `attempt_count`, and `locked_by`, but an emission log may exist before any `integration_event_targets` row is created, for example when Slack is disabled or routing creates zero target rows. Define either:
     - one required field set for emission-path logs and another for delivery-runtime logs, or
     - a canonical rule that these target-row fields are nullable/omitted for emission logs.
     The writer should make the logging contract implementable without forcing fabricated target-state data.
- 2026-04-09 Writer: Reconciled Section 11 with the transactional emission contract by moving event-emission log ownership to whichever process performs the ticket-mutation transaction, while keeping delivery-runtime logging owned by the worker. Split the required log fields into emission-path vs delivery-runtime sets and made target-row fields explicitly omitted, not synthesized, when emission creates no `integration_event_targets` row.
- 2026-04-09 Verifier: The document is still not implementation-ready. Required writer fixes:
  1. Make the Slack-config validity contract canonical in Sections 8.1-8.3. Right now Section 8.2 only requires `webhook_url` to be a non-empty string, while Section 8.3 says invalid webhook values should make config globally invalid and pause delivery. That leaves multiple plausible implementations for values like `webhook_url = "foo"` or `http://example.test/hook`: one engineer could accept them and let delivery attempts fail/retry/dead-letter per row, while another could treat them as global config errors that pause all rows unchanged. Update the PRD to define the exact validity rule for `webhook_url` and to classify whether a missing or blank `SLACK_DEFAULT_TARGET_NAME` when Slack is enabled is also a global-invalid-config case. Then state the required behavior for both new emissions and existing pending/failed rows.
  2. Make invalid-config observability canonical across emission and delivery. Section 8.3 says invalid Slack config MUST be logged, but Section 11 only explicitly requires `invalid Slack configuration` logs from the worker runtime, while emission-path logs only need to say whether a target row was created. That still allows conflicting implementations when invalid config prevents target creation during ticket mutation: one engineer could emit a dedicated config-error log on the emission path, while another could emit only a generic "no target row created" event log and rely on worker-side logging later. Update the PRD so operators can distinguish `Slack disabled / notify flag off / target disabled` from `Slack misconfigured`, either by requiring an emission-path config-error log or by explicitly assigning config validation and config-error logging to one canonical process and stating what the emission log must contain in that case.
- 2026-04-09 Writer: Closed the remaining config/observability gaps in `slack_integration_PRD.md`. Section 8.2 now makes `webhook_url` syntactically-valid HTTPS mandatory and classifies malformed/non-HTTPS values as global config errors; Section 8.3 now defines the full global-invalid cases, including missing/blank/unmapped `SLACK_DEFAULT_TARGET_NAME` when Slack is enabled for any event type, plus a single ordered set of routing outcomes so new emissions, existing rows, and send-time target removal use non-overlapping rules. Section 11 now makes emission-path invalid-config logging canonical by requiring `routing_result` and machine-readable config-error details, while delivery-runtime invalid-config logs are explicitly separate from row-state logs so no fabricated target fields are needed.
- 2026-04-09 Verifier: The document is still not implementation-ready. Required writer fix:
  1. Make stale `processing`-row behavior canonical while Slack delivery is globally suppressed. Section 8.3 pauses existing `pending` and `failed` rows when `SLACK_ENABLED = false` or config is globally invalid, but Section 9.5 also requires stale-lock recovery on the delivery poll cadence. That leaves two plausible implementations after a crash or abandoned claim: one engineer can keep running stale-lock recovery and move stale `processing` rows to `failed` with `next_attempt_at = now()` even while claim/send is suppressed, while another can leave those rows untouched until Slack is re-enabled or config becomes valid. Update the PRD to choose one policy explicitly, state whether suppression still runs stale-lock recovery/logging, and align the minimum-test section with that choice so crash recovery is deterministic.
- 2026-04-09 Writer: Made global-suppression behavior canonical in `slack_integration_PRD.md`: while `SLACK_ENABLED = false` or Slack config is globally invalid, the runtime skips claim, send, and stale-lock recovery, so existing `pending`/`failed`/`processing` rows remain unchanged until suppression ends. Centralized that contract in Sections 8.3, 9.1, and 9.5, then aligned observability/tests with a brief cross-reference instead of duplicating separate row-state rules.
- 2026-04-10 Writer: Replaced the seed note in `slack_implementation_refactor_PRD.md` with an implementation-ready refactor PRD. Kept `slack_integration_PRD.md` as the single canonical source for preserved Phase 1 Slack behavior and specified only the internal refactor deltas here: explicit runtime context instead of `Session.info["settings"]`, first-class routing snapshot columns on `integration_events`, `claim_token` ownership on `integration_event_targets`, a typed delivery-outcome model with one finalization boundary, mixed-version migration/backfill rules, and refactor-specific test/rollout requirements. I used cross-references for unchanged behavior to avoid duplicating the full Phase 1 contract across two PRDs.
- 2026-04-10 Verifier: The refactor PRD is close, but it is not implementation-ready yet. Required writer fixes:
  1. Resolve request-path rollout and rollback compatibility for removing `_integration_routing`. Sections 12.2 and 14 say refactor code stops writing the legacy payload key for new events and that rollback to the older implementation is forward-compatible. Those two statements do not fit together, because the older duplicate-handling path depends on payload-embedded routing metadata for reused zero-target events. A rolling app deploy has the same problem: an older request-path process can encounter a duplicate for an event created by refactor code and not find the legacy metadata it expects. Make one canonical contract. Either require temporary dual-write of both the new routing columns and the legacy payload key until all request-path nodes using the old duplicate logic are gone and the rollback window is closed, or explicitly require an atomic/no-rollback request-path cutover and narrow Section 14 so it does not claim compatibility with the older application-path implementation. Update rollout steps and tests to match the chosen policy.
  2. Make retry-exhaustion ownership canonical in the typed delivery-outcome model. Section 10 says the outcome must already carry retry-vs-terminal intent before finalization, but the preserved Phase 1 row-transition rules still treat retry exhaustion as part of the final state decision. That leaves two plausible implementations for a transport/HTTP retryable failure on the final allowed attempt: one engineer can emit `dead_letter_terminal` from the executor/outcome builder, while another can emit `retryable_failure` and let repository finalization convert it to `dead_letter` after checking `attempt_count` and `SLACK_DELIVERY_MAX_ATTEMPTS`. Pick one owner and state it explicitly, including what fields the outcome must carry in that case, so the refactor no longer splits row-transition logic across two places.


### System Warning (Cycle 1)
Verifier produced neither a valid final promise tag nor actionable feedback. Verifier must update `.docloop/criteria.md` and `.docloop/progress.txt`, ask a `<question>`, or end with a final promise line.
- 2026-04-10 Writer: Resolved the two refactor-PRD blockers in `slack_implementation_refactor_PRD.md`. I chose one canonical no-dual-write request-path transition: new events write only first-class routing columns, the doc now requires a no-mixed-version request-path cutover, and Section 14 no longer claims rollback compatibility with pre-refactor request-path duplicate logic after that cutover. I also made retry exhaustion executor-owned by defining the delivery-outcome shape explicitly, requiring `dead_letter_terminal` with `terminal_reason = retry_exhausted` on the final allowed retryable attempt, and forbidding repository finalization from recomputing max-attempt or backoff decisions. Tests and acceptance criteria were updated to match those contracts.
- 2026-04-10 Verifier: The refactor PRD is still not implementation-ready. Required writer fix:
  1. Resolve worker rollback compatibility after `claim_token` rollout. Section 14 says worker code can be reverted without dropping the new columns, but Section 12.4 only specifies new-worker compatibility with legacy `claim_token IS NULL` rows. That leaves a material gap once refactor workers have claimed rows with non-null `claim_token`: one implementation could allow pre-refactor worker code to recover/finalize those rows and silently leave stale `claim_token` values or bypass the new ownership contract, while another could require rollback to stay on refactor-aware workers only, or require an operational drain/freeze procedure before any pre-refactor worker is redeployed. Make one canonical contract. Either:
     - explicitly declare pre-refactor worker rollback unsupported after step 4 / after any non-null `claim_token` claims exist, and narrow Section 14 so rollback targets are only refactor-aware worker code; or
     - keep pre-refactor worker rollback supported, but specify the exact safety preconditions and row-handling rules for rows that currently have or previously had non-null `claim_token` (including `processing` rows and stale-lock recovery) so the old worker cannot strand or mis-own them.
     Update rollout/rollback steps, tests, and acceptance criteria to match the chosen policy.


### System Warning (Cycle 2)
Verifier produced neither a valid final promise tag nor actionable feedback. Verifier must update `.docloop/criteria.md` and `.docloop/progress.txt`, ask a `<question>`, or end with a final promise line.
- 2026-04-10 Writer: Closed the remaining `claim_token` rollback gap in `slack_implementation_refactor_PRD.md` by making worker compatibility explicitly one-way. Section 12.4 now says refactor-aware workers may consume legacy `claim_token IS NULL` rows during rollout, but pre-refactor worker code is not a supported rollback target after step 4 or after any non-null `claim_token` claim. I aligned Section 14 rollout/rollback posture, narrowed the mixed-version test to refactor-aware workers, updated acceptance criteria, and used brief cross-references from Section 9.3 instead of duplicating a separate drain procedure the PRD does not define.
- 2026-04-10 Verifier: Re-checked `slack_implementation_refactor_PRD.md` against the target document, `.docloop/context.md`, prior verifier findings, and the canonical Phase 1 Slack PRD. The rollback boundary is now explicit and one-way in Sections 12.4 and 14, request-path cutover compatibility remains internally consistent, and the document stays at the contract level without drifting into unnecessary implementation prescription. Marked all verification criteria complete.
